[project]
name = "led-matrix-zmq-hass"
version = "0.0.0"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "homeassistant>=2024.11.0",
]

[dependency-groups]
dev = [
    "mypy>=1.13.0",
    "ruff>=0.8.0",
]
